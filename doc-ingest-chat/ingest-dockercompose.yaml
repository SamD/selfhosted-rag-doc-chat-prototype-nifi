# --- REUSABLE ANCHORS ---

x-base-service: &base-service
  restart: unless-stopped
  env_file:
    - .env
  volumes:
    - .:/app
    - ../shared:/app/shared
    - ./.env:/app/.env
    - ${DEFAULT_DOC_INGEST_ROOT}:${DEFAULT_DOC_INGEST_ROOT}
    - ${STAGING_DIR}:${STAGING_DIR}
    - ${PREPROCESSING_DIR}:${PREPROCESSING_DIR}
    - ${INGESTION_DIR}:${INGESTION_DIR}
    - ${CONSUMING_DIR}:${CONSUMING_DIR}
    - ${SUCCESS_DIR}:${SUCCESS_DIR}
    - ${FAILED_DIR}:${FAILED_DIR}
    - ${EMBEDDING_ENDPOINTS_MOUNT}:${EMBEDDING_ENDPOINTS_MOUNT}:ro
    - ${LLM_PATH_MOUNT}:${LLM_PATH_MOUNT}:ro
    - ${SUPERVISOR_LLM_ENDPOINTS_MOUNT}:${SUPERVISOR_LLM_ENDPOINTS_MOUNT}:ro
    - ${WHISPER_MODEL_ENDPOINTS_MOUNT:-/tmp}:${WHISPER_MODEL_ENDPOINTS_MOUNT:-/tmp}:ro
    - ${TOKENIZER_MODEL_PATH}:${TOKENIZER_MODEL_PATH}:ro

x-gpu-params: &gpu-params
  runtime: nvidia
  shm_size: '2gb'
  build:
    context: ..
    dockerfile: doc-ingest-chat/Dockerfile.worker
    args:
      TARGET_TYPE: cuda
      BUILDKIT_INLINE_CACHE: 1
      OCR_ENDPOINTS: ${OCR_ENDPOINTS:-LOCAL}
  environment:
    - PYTHONUNBUFFERED=1
    - PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION=python
    - REDIS_HOST=${REDIS_HOST}
    - REDIS_PORT=${REDIS_PORT:-6379}
    - DEFAULT_DOC_INGEST_ROOT=${DEFAULT_DOC_INGEST_ROOT}
    - EMBEDDING_ENDPOINTS=${EMBEDDING_ENDPOINTS}
    - TOKENIZER_MODEL_PATH=${TOKENIZER_MODEL_PATH}
    - LLM_PATH=${LLM_PATH}
    - SUPERVISOR_LLM_ENDPOINTS=${SUPERVISOR_LLM_ENDPOINTS}
    - SUPERVISOR_MAX_TOKENS=${SUPERVISOR_MAX_TOKENS:-4096}
    - WHISPER_MODEL_ENDPOINTS=${WHISPER_MODEL_ENDPOINTS}
    - OCR_ENDPOINTS=${OCR_ENDPOINTS}
    - VECTOR_DB_URL=${VECTOR_DB_URL:-}
    - VECTOR_DB_PROFILE=${VECTOR_DB_PROFILE}
    - VECTOR_DB_USE_GRPC=${VECTOR_DB_USE_GRPC:-true}
    - VECTOR_DB_GRPC_PORT=${VECTOR_DB_GRPC_PORT:-6334}
    - VECTOR_DB_TIMEOUT=${VECTOR_DB_TIMEOUT:-60.0}
    - VECTOR_DB_BATCH_SIZE=${VECTOR_DB_BATCH_SIZE:-20}
    - PDF_FORCE_OCR=${PDF_FORCE_OCR:-false}
    - FORCE_MARKDOWN_LLM=${FORCE_MARKDOWN_LLM:-false}
    - HA_INTERLEAVE=${HA_INTERLEAVE:-}
    - HAPROXY_SUPERVISOR_ENDPOINTS=${HAPROXY_SUPERVISOR_ENDPOINTS:-}
    - HAPROXY_EMBEDDING_ENDPOINTS=${HAPROXY_EMBEDDING_ENDPOINTS:-}
    - HAPROXY_WHISPER_ENDPOINTS=${HAPROXY_WHISPER_ENDPOINTS:-}
    - HAPROXY_OCR_ENDPOINTS=${HAPROXY_OCR_ENDPOINTS:-}
    - LD_LIBRARY_PATH=/usr/local/cuda/lib64:/usr/local/nvidia/lib64:/usr/lib/x86_64-linux-gnu
    - NVIDIA_VISIBLE_DEVICES=all
    - NVIDIA_DRIVER_CAPABILITIES=compute,utility

services:
  qdrant:
    image: qdrant/qdrant:v1.12.2
    profiles: [local-qdrant]
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
      - QDRANT__SERVICE__HTTP_PORT=6333
      - QDRANT__SERVICE__GRPC_PORT=6334
      - QDRANT__STORAGE__STORAGE_PATH=/qdrant/storage
      - QDRANT__LOG_LEVEL=DEBUG
      - QDRANT__TELEMETRY_DISABLED=true
    ports:
      - "${VECTOR_DB_PORT:-9002}:6333"
      - "${VECTOR_DB_GRPC_PORT:-9003}:6334"
    volumes:
      - "${VECTOR_DB_DATA_DIR:-./Docs/qdrant_data}:/qdrant/storage"
    networks:
      default:
        aliases:
          - vector-db
    restart: unless-stopped

  haproxy_supervisor:
    image: haproxy:2.8-alpine
    container_name: haproxy_supervisor
    env_file:
      - .env
    environment:
      - SERVICE=supervisor
      - SUPERVISOR_LLM_ENDPOINTS=${HAPROXY_SUPERVISOR_ENDPOINTS:-}
    volumes:
      - ../infra/haproxy-entrypoint.sh:/entrypoint.sh:ro
    entrypoint: ["/bin/sh", "/entrypoint.sh", "supervisor"]
    ports:
      - "11437:11437"
      - "8404:8404"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8404/stats"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 5s

  haproxy_embd:
    image: haproxy:2.8-alpine
    container_name: haproxy_embd
    env_file:
      - .env
    environment:
      - SERVICE=embedding
      - EMBEDDING_ENDPOINTS=${HAPROXY_EMBEDDING_ENDPOINTS:-}
      - TOKENIZER_MODEL_PATH=${TOKENIZER_MODEL_PATH}
    volumes:
      - ../infra/haproxy-entrypoint.sh:/entrypoint.sh:ro
    entrypoint: ["/bin/sh", "/entrypoint.sh", "embedding"]
    ports:
      - "11438:11438"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8405/stats"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 5s

  haproxy_whisper:
    image: haproxy:2.8-alpine
    container_name: haproxy_whisper
    env_file:
      - .env
    environment:
      - SERVICE=whisper
      - WHISPER_MODEL_ENDPOINTS=${HAPROXY_WHISPER_ENDPOINTS:-}
    volumes:
      - ../infra/haproxy-entrypoint.sh:/entrypoint.sh:ro
    entrypoint: ["/bin/sh", "/entrypoint.sh", "whisper"]
    ports:
      - "11439:11439"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8406/stats"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 5s

  haproxy_ocr:
    image: haproxy:2.8-alpine
    container_name: haproxy_ocr
    env_file:
      - .env
    environment:
      - SERVICE=ocr
      - OCR_ENDPOINTS=${HAPROXY_OCR_ENDPOINTS:-}
    volumes:
      - ../infra/haproxy-entrypoint.sh:/entrypoint.sh:ro
    entrypoint: ["/bin/sh", "/entrypoint.sh", "ocr"]
    ports:
      - "11440:11440"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8407/stats"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 5s

  consumer_worker_gpu:
    <<: [*base-service, *gpu-params]
    container_name: consumer_worker_gpu
    profiles: [cuda-qdrant]
    command: ["python", "run_consumer.py"]

  ocr_worker:
    <<: [*base-service, *gpu-params]
    container_name: ocr_worker
    profiles: [cuda]
    command: ["python", "run_ocr_worker.py"]
    healthcheck:
      test: ["CMD", "pgrep", "-f", "run_ocr_worker.py"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

  retype_llm_worker:
    <<: [*base-service, *gpu-params]
    container_name: retype_llm_worker
    profiles: [cuda]
    command: ["python", "run_retype_llm_worker.py"]
    healthcheck:
      test: ["CMD", "pgrep", "-f", "run_retype_llm_worker.py"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

  whisperx_worker:
    <<: [*base-service]
    container_name: whisperx_worker
    profiles: [cuda]
    runtime: nvidia
    build:
      context: ..
      dockerfile: doc-ingest-chat/Dockerfile.whisperx
    entrypoint: ["python", "run_whisperx_worker.py"]
    environment:
      - PYTHONUNBUFFERED=1
      - PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION=python
      - PYTHONPATH=/app
      - REDIS_HOST=${REDIS_HOST}
      - REDIS_PORT=${REDIS_PORT:-6379}
      - DEFAULT_DOC_INGEST_ROOT=${DEFAULT_DOC_INGEST_ROOT}
      - EMBEDDING_ENDPOINTS=${EMBEDDING_ENDPOINTS}
      - TOKENIZER_MODEL_PATH=${TOKENIZER_MODEL_PATH}
      - LLM_PATH=${LLM_PATH}
      - SUPERVISOR_LLM_ENDPOINTS=${SUPERVISOR_LLM_ENDPOINTS}
      - SUPERVISOR_MAX_TOKENS=${SUPERVISOR_MAX_TOKENS:-4096}
      - WHISPER_MODEL_ENDPOINTS=${WHISPER_MODEL_ENDPOINTS}
      - OCR_ENDPOINTS=${OCR_ENDPOINTS}
      - VECTOR_DB_URL=${VECTOR_DB_URL:-}
      - VECTOR_DB_PROFILE=${VECTOR_DB_PROFILE}
      - VECTOR_DB_USE_GRPC=${VECTOR_DB_USE_GRPC:-true}
      - VECTOR_DB_GRPC_PORT=${VECTOR_DB_GRPC_PORT:-6334}
      - VECTOR_DB_TIMEOUT=${VECTOR_DB_TIMEOUT:-60.0}
      - VECTOR_DB_BATCH_SIZE=${VECTOR_DB_BATCH_SIZE:-20}
      - PDF_FORCE_OCR=${PDF_FORCE_OCR:-false}
      - DEVICE=cuda
      - COMPUTE_TYPE=float16

  gatekeeper_worker:
    <<: [*base-service, *gpu-params]
    container_name: gatekeeper_worker
    profiles: [cuda]
    command: ["python", "run_gatekeeper.py"]
    depends_on:
      ocr_worker:
        condition: service_healthy
      haproxy_supervisor:
        condition: service_healthy
        required: false

  producer_worker:
    <<: [*base-service, *gpu-params]
    container_name: producer_worker
    profiles: [cuda]
    command: ["python", "run_producer.py"]
    depends_on:
      ocr_worker:
        condition: service_healthy

  api_gpu_qdrant:
    <<: [*base-service, *gpu-params]
    container_name: api_gpu
    profiles: [cuda-qdrant]
    command: ["python", "apimain.py"]
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ../astro-frontend
      dockerfile: Dockerfile.frontend
      target: dev
    profiles: [with-frontend]
    ports:
      - "4321:4321"
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
      - NODE_ENV=development
      - PUBLIC_API_BASE_URL=${PUBLIC_API_BASE_URL:-http://localhost:8000/api/v1}
    command: ["./node_modules/.bin/astro", "dev", "--host", "0.0.0.0", "--port", "4321"]
    volumes:
      - ../astro-frontend:/app
      - /app/node_modules
      - /app/.astro
